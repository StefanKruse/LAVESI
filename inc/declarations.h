#pragma once

#include <list>
#include <vector>
#include "structures.h"

using namespace std;

// plot dimensions rows==y, cols==x
const int treerows = 100000;
const int treecols = 20;

// parameter input
const int stringlengthmax = 255;
char divisionsign[] = "=;";
struct Parameter parameter[1];
